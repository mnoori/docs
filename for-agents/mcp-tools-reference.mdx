---
title: "MCP tools reference"
sidebarTitle: "Tools reference"
description: "Generated reference for Patchline's product MCP tool inventory. The live tools/list response is the source of truth."
icon: "list-tree"
keywords:
  - patchline mcp tools
  - mcp tools reference
  - mcp tool schemas
  - aria tool list
---

import { AICallout } from "/snippets/ai-callout.mdx"
import { TierBadge } from "/snippets/tier-badge.mdx"

<!-- AUTO-GENERATED by scripts/generate-mcp-tools-reference.mjs from the product MCP tools/list shape. Do not edit tool tables by hand. -->

This page is the human-readable snapshot of Patchline's product MCP
tools. The exact machine-readable schema comes from `tools/list` on:

```text
https://www.patchline.ai/api/mcp/v1
```

<TierBadge tier="all" /> &nbsp; The MCP endpoint is available on every tier. Individual actions may still be shaped by workspace limits and feature availability.

## Generated Inventory

Generated on **May 27, 2026** from the product MCP registry that backs
`tools/list`.

| Group | Tools |
|---|---:|
| Dispatch | 1 |
| Catalog | 3 |
| Audio Upload | 2 |
| Projects | 3 |
| Artists | 4 |
| Releases | 1 |
| Playlists | 2 |
| Storefront | 1 |
| Public Surfaces | 6 |
| Music Data | 3 |
| AI Generation | 2 |
| Roster | 2 |
| Similarity | 1 |

Total: **31 tools**.

## Dispatch

<a id="analyze_url" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `analyze_url` | Read | `url` | Parses Spotify or YouTube URLs and returns the canonical identity plus suggested next MCP tools. |

## Catalog

<a id="browse_catalog" />
<a id="catalog_search" />
<a id="get_asset" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `browse_catalog` | Read | `page`, `pageSize` | Lists tracks in the user's catalog with pagination. |
| `catalog_search` | Read | `query`, `limit` | Searches catalog tracks by title, artist, ISRC, and genre. |
| `get_asset` | Read | `assetId`, `mode` | Retrieves a catalog track by asset ID; `mode` controls summary vs. full enrichment payload. |

## Audio Upload

<a id="get_asset_upload_link" />
<a id="confirm_asset_upload" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `get_asset_upload_link` | Write | `fileName`, `fileSizeBytes`, `trackTitle`, `artistId`, `artistName`, `projectName`, `format` | Creates an Asset Hub record and returns a short-lived upload URL for audio bytes. |
| `confirm_asset_upload` | Write | `assetId` | Confirms the upload and triggers Patchline track analysis. |

## Projects

<a id="create_project" />
<a id="get_artist_context" />
<a id="create_campaign" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `create_project` | Write | `artistId`, `title`, `type`, `distributionMode`, `ideaMetadata` | Creates a Patchline Project Anchor for a release or campaign workspace. |
| `get_artist_context` | Read | `artistId`, `artistName`, `projectId` | Reads connected campaign context such as platforms, defaults, and recent releases. |
| `create_campaign` | Write | `projectId`, `title`, `artist`, `releaseType`, `genre`, `releaseDate`, `description`, `artistId`, `marketingGoal`, `goals`, `targetStreams`, `budget`, `promoTier`, `desiredAssets`, `visualReferences`, `notes`, `campaignIntake` | Generates and persists campaign tasks for a Patchline project. |

## Artists

<a id="get_artist_intelligence" />
<a id="browse_roster" />
<a id="search_artists" />
<a id="get_trending_artists" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `get_artist_intelligence` | Read | `artist_name`, `mode` | Reads cached Aria intelligence for a connected artist. |
| `browse_roster` | Read | `query`, `limit` | Lists or filters the authenticated user's connected artist roster. |
| `search_artists` | Read | `query`, `limit` | Searches the user's roster and Patchline's artist intelligence index. |
| `get_trending_artists` | Read | `genre`, `markets`, `min_monthly_listeners`, `max_monthly_listeners`, `limit` | Finds trending artists with optional genre, market, and listener filters. |

## Releases

<a id="get_releases" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `get_releases` | Read | `status`, `limit` | Lists the user's releases with status, release date, UPC, track count, and cover art. |

## Playlists

<a id="find_playlists" />
<a id="inspect_playlist" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `find_playlists` | Read | `assetId`, `spotifyUrl`, `limit` | Matches a track to curated Spotify playlists using audio and genre scoring. |
| `inspect_playlist` | Read | `playlist_url`, `spotifyPlaylistId`, `spotify_playlist_id`, `playlistId`, `include_tracks`, `include_curator_network` | Reads playlist intelligence for a Spotify playlist URL or ID. |

## Storefront

<a id="create_smart_link" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `create_smart_link` | Write | `assetId`, `title`, `enabledSections`, `projectId`, `campaignId`, `taskId` | Creates a Patchline smart link for a released catalog track with an ISRC. |

## Public Surfaces

<a id="create_drop_link" />
<a id="create_pitch_link" />
<a id="create_asset_share" />
<a id="create_media_kit" />
<a id="create_briefing" />
<a id="read_public_surface" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `create_drop_link` | Write | `releaseId`, `title`, `artist`, `coverArt`, `releaseDate`, `genre`, `label`, `message`, `expiresInDays`, `enabledSections`, `projectId`, `campaignId`, `taskId`, `spotifyPresaveUrl`, `applePresaveUrl`, `smartLinkUrl`, `storeUrl` | Creates a public release/drop link for an owned release. |
| `create_pitch_link` | Write | `trackAssetId`, `trackName`, `artistName`, `targetCuratorName`, `pitchCopy`, `targetPlaylist`, `fitRationale`, `attachedAssetShareId`, `coverArt`, `audioPreviewUrl`, `expiresInDays`, `enabledSections`, `projectId`, `campaignId`, `taskId` | Creates a public curator pitch link for an owned track. |
| `create_asset_share` | Write | `assetId`, `message`, `expiresInDays`, `maxDownloads`, `enabledSections` | Creates a public asset share link for an owned catalog asset. |
| `create_media_kit` | Write | `artistId`, `enabledSections`, `selectedTrackIds`, `format` | Creates a public artist media-kit page for an owned roster artist. |
| `create_briefing` | Write | `title`, `report`, `sections`, `message`, `sharedBy`, `expiresInDays`, `enabledSections`, `projectId`, `campaignId`, `taskId` | Creates a public briefing or report share link. |
| `read_public_surface` | Read | `url`, `shareId` | Reads owner-safe metadata and section configuration for a public surface. |

## Music Data

<a id="get_audio_features" />
<a id="get_work_metadata" />
<a id="get_song_intelligence" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `get_audio_features` | Read | `assetId`, `isrc`, `spotifyTrackUrl` | Gets BPM, key, energy, moods, genres, and other audio features. |
| `get_work_metadata` | Read | `spotifyId`, `spotifyTrackUrl`, `isrc`, `songName`, `artistName` | Gets songwriting and publishing metadata for a track. |
| `get_song_intelligence` | Read | `assetId`, `isrc`, `spotifyTrackUrl`, `mode` | Reads song-level intelligence: identifiers, metrics, playlists, charts, tags, timeseries, and work credits when available. |

## AI Generation

<a id="get_bio" />
<a id="generate_pitch" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `get_bio` | Read | `artist_name` | Returns the cached artist bio created during enrichment; it does not synthesize a new bio on demand. |
| `generate_pitch` | Read | `artist_name`, `track_name`, `target_playlist` | Generates a Spotify editorial pitch using available Aria intelligence. |

## Roster

<a id="add_artist" />
<a id="remove_artist" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `add_artist` | Write | `artist_url`, `artist_name` | Adds an artist to the user's Aria roster from a platform URL and starts enrichment when possible. |
| `remove_artist` | Destructive | `artist_id` | Removes an artist from the user's Aria roster. |

## Similarity

<a id="find_similar_tracks" />

| Tool | Mode | Inputs | What it does |
|---|---|---|---|
| `find_similar_tracks` | Read | `assetId`, `limit` | Finds catalog tracks with similar audio characteristics to a given asset. |

## Common Patterns

### User pasted a URL

Call `analyze_url` first. It returns a canonical identity and suggested
next tools with the argument shape to use next.

### User wants to upload audio through Aria

Use `get_asset_upload_link`, have the client upload the file bytes to
the returned URL, then call `confirm_asset_upload`. Track analysis
starts after confirmation.

### User asks for a roster list

Use `browse_roster`. Use `get_artist_intelligence` only after the user
chooses a specific artist or asks for deeper context.

### User asks for playlist strategy

For catalog tracks, start with `get_asset` or `get_song_intelligence`,
then call `find_playlists`. For a specific playlist URL, use
`inspect_playlist`.

## Regenerate This Page

Run the generator from the docs repo with an MCP credential:

```bash
PATCHLINE_MCP_API_KEY="..." node scripts/generate-mcp-tools-reference.mjs
```

or:

```bash
PATCHLINE_MCP_BEARER="..." node scripts/generate-mcp-tools-reference.mjs
```

The generator calls the live MCP endpoint, runs `tools/list`, and
rewrites this page. Do not update the tool tables manually.

## Related Pages

- [MCP overview](/for-agents/mcp-overview) - what the Patchline MCP is for
- [Install in your AI tool](/for-agents/mcp-install) - per-client install
- [Agent discovery files](/for-agents/agents-md-explained) - how agents find the docs
- [Aria overview](/aria/overview) - same product intelligence in chat form

<AICallout page="/for-agents/mcp-tools-reference" />
